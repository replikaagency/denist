# Vercel deployment

- **Root Directory:** set to `codigo` in the Vercel project (this repo’s Next.js app and `package.json` live here, not at the repository root).
- **Environment variables:** copy from `.env.example` into Vercel (Production). Use your real production URL for `NEXT_PUBLIC_APP_URL`.
